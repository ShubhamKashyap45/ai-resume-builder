Generate a professional IT job resume in JSON format based on the following description. Ensure the JSON is consistent, well-structured, and includes all specified keys, even if some values are empty or null.

Important Notes:
Use the exact key names and maintain the hierarchy shown below.
For all fields named year (such as in graduationYear, certifications, achievements), output the full date in dd-mm-yyyy format to support calendar selection on the frontend.

Input Description:
"{{userDescription}}"

JSON Structure Requirements:
{
  "personalInformation": {
    "fullName": "string",
    "email": "string",
    "phoneNumber": "string",
    "location": "string",
    "linkedIn": "string or null",
    "gitHub": "string or null",
    "portfolio": "string or null"
  },
  "summary": "string",
  "skills": [
    {
      "title": "string",
      "level": "string"
    }
  ],
  "experience": [
    {
      "jobTitle": "string",
      "company": "string",
      "location": "string",
      "durationStart": "string (e.g., 'Jan 2020 - Present')",
      "durationEnd": "string (e.g., 'Jan 2025 or Present')",
      "responsibility": "string"
    }
  ],
  "education": [
    {
      "degree": "string",
      "university": "string",
      "location": "string",
      "graduationYear": "string (format: 'dd-mm-yyyy')"
    }
  ],
  "certifications": [
    {
      "title": "string",
      "issuingOrganization": "string",
      "duration": "string (format: 'dd-mm-yyyy')"
    }
  ],
  "projects": [
    {
      "title": "string",
      "description": "string",
      "technologiesUsed": ["string"],
      "githubLink": "string or null"
    }
  ],
  "achievements": [
    {
      "title": "string",
      "year": "string",
      "extraInformation": "string"
    }
  ],
  "languages": [
    {
      "id": number,
      "name": "string"
    }
  ],
  "interests": [
    {
      "id": number,
      "name": "string"
    }
  ]
}